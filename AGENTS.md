# UAV-RT — project hub

**The agent instructions for this project live in [`CLAUDE.md`](CLAUDE.md). Read that file.**

It is written for any AI agent working in this repository, not just Claude — Codex, Copilot,
Cursor, or a human. This file exists only because some tools look for `AGENTS.md` by name.

Do not duplicate the instructions here. A second copy drifts out of date, and keeping one
authoritative context document is the entire point of this hub repository.

After `CLAUDE.md`, the next thing to read is `docs/SOFTWARE_CONTEXT.md` — the authoritative map
of the system.
